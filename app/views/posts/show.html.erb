<h1><%= @post.title %></h1>

<div><%= @post.body%></div>
<br>
<div class= "button"><%= link_to "Edit Post", edit_post_path(@post) %></div>
<div class= "button"><%= link_to "Delete", post_path(@post), method: :delete, data: {confirm: "Are you sure?"} %></div>